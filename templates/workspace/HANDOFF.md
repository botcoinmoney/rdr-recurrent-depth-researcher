# Handoff

This file is updated automatically after each cycle.

Start with the newest `reports/cycle-*.md` file, then check `findings.md`, `workspace_state.yaml`, and the latest run directory under `runs/`.
